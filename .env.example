# Copy to .env and fill in. .env is git-ignored.

# Required: the main alert webhook.
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/XXXX/YYYY

# Optional: a separate webhook for heartbeat status messages.
# If unset, heartbeats go to DISCORD_WEBHOOK_URL.
DISCORD_HEARTBEAT_WEBHOOK_URL=
